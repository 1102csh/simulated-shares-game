#pragma once

int count(int c);		//횟수 카운트해서 저장하는 함수. sum 변수로 리턴해준다.
int minigame(int ownmoney);		//메인 게임함수. 문장 출력하고 입력받는다.
int earn(int sum, int hard);		// 알바비를 계산하고 지급해주는 함수
int add(int a, int ownmoney);			//알바비를 보유 현금에 더해주는 함수